# Copy to .env and adjust. Never commit real credentials.

# PostgreSQL
DB_HOST=localhost
DB_PORT=5432
DB_NAME=libris
DB_USER=libris
DB_PASSWORD=libris_dev

# Backend
SERVER_PORT=8080
COOKIE_SECURE=false
API_DOCS_ENABLED=true

# Mail (MailHog in development)
MAIL_HOST=localhost
MAIL_PORT=1025
